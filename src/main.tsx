import React from 'react'
import ReactDOM from 'react-dom/client'
import './style.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>Project transition in progress. Please see nigeria_schools.html for the active map.</div>
  </React.StrictMode>,
)
